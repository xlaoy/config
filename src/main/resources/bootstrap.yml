spring:
  application:
    name: config
  cloud:
    config:
      server:
        git:
          uri: https://github.com/xlaoy/springconfig-repo.git
          search-paths: /
          username: xlaoy
          password: zhangyijun123
        prefix: config