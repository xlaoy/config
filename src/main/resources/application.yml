server:
  port: 9940
  tomcat:
    uri-encoding: UTF-8

spring:
  http:
    encoding:
      charset: UTF-8
      force: true

# config 配置对称加密
encrypt:
  key: wocaofuck

#打开actuator的所有端点
management:
  endpoints:
    web:
      exposure:
        include: "*"